// component to add a game
import React from 'react';

class GameAdd extends React.Component {
    constructor(props) {
        super(props)
        this.state = {
            // you should save the variables in this state
        }
    }

    render() {
        return (
            <div>
                This is the Game Add component.
            </div>
        )
    }
}

export default GameAdd;